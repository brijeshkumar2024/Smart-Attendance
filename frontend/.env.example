# Backend API base URL (without trailing /api is also accepted)
VITE_API_BASE_URL=http://localhost:5000

# Optional explicit socket URL. Defaults to API host when omitted.
VITE_SOCKET_URL=http://localhost:5000

# Optional API timeout in milliseconds. Increase for cold-start hosting.
VITE_API_TIMEOUT_MS=30000
